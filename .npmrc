# shadcn/ui and Next.js expect hoisted React types across workspace packages.
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*

# Fail fast in CI instead of silently resolving a different tree.
engine-strict=true
