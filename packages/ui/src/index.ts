/**
 * @vyora/ui — design system.
 *
 * Scaffolded in Phase 2 (project structure). The token + component spec in
 * `design/Vyora Design System.dc.html` is implemented in Phase 4; only the
 * class-merging helper exists so far.
 */
export { cn } from "./lib/cn.js";
